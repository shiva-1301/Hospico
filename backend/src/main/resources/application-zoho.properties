# Zoho Data Store Configuration
zoho.enabled=true
zoho.project.id=26566000000013009
zoho.env.id=60061261997
zoho.region=accounts.zoho.in
zoho.users.table.id=26566000000019177
zoho.client.id=${ZOHO_CLIENT_ID}
zoho.client.secret=${ZOHO_CLIENT_SECRET}
# Refresh token is less critical if PAT is provided, but good to have if PAT fails/expires and we have logic for it.
# However, the user didn't provide a refresh token value in the screenshot (it was hidden or not there?), 
# actually the screenshot variables list doesn't show ZOHO_REFRESH_TOKEN. It shows PAT.
# So I will leave refresh token empty or commented.
zoho.refresh.token=${ZOHO_REFRESH_TOKEN}
zoho.pat.token=${ZOHO_PAT_TOKEN:m_1004.0f5730763d2dab1c71f660b96b89ccd3.9ae8b37b500909a853e7d3d80916d9bb}

# Disable JPA auto-DDL when using Zoho
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# Keep PostgreSQL config for fallback/migration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/hospitaldb}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
